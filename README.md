# BookNLP GPU Service

**BookNLP GPU microservice** - FastAPI сервер для обработки текстов через BookNLP на видеокарте.

## 🎯 Назначение

Отдельный сервис для запуска на машине с GPU. Принимает тексты по HTTP API, обрабатывает через BookNLP big model на видеокарте, возвращает результаты.

## 📋 Структура

```
booknlp-gateway/
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile              # Образ контейнера
├── booknlp_server.py       # FastAPI сервер
├── test_service.sh         # Полный тест сервиса
├── quick_check.sh          # Быстрая проверка
├── .env.example            # Пример конфигурации
├── .gitignore
└── README.md
```

## 🚀 Быстрый старт

### 1. Настройка

```bash
# Скопируйте .env.example в .env
cp .env.example .env

# Отредактируйте пути к моделям и данные
nano .env
```

### 2. Запуск

```bash
docker compose up -d
```

### 3. Проверка

**Быстрая проверка:**
```bash
./quick_check.sh
```

**Полный тест:**
```bash
./test_service.sh
```

**Или вручную:**
```bash
# Health check
curl http://localhost:8888/health | jq '.'

# Должен вернуть информацию о GPU
```

## ⚙️ Конфигурация через .env

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `MODELS_PATH` | Путь к моделям на хосте | `./models` |
| `DATA_PATH` | Путь для данных | `./data` |
| `BOOKNLP_MODEL` | Размер модели (small/big) | `big` |
| `GPU_DEVICE` | Номер GPU | `0` |
| `API_PORT` | Порт API | `8888` |
| `MODEL_IDLE_TIMEOUT` | Таймаут выгрузки модели (секунды) | `300` (5 мин) |

## 💡 Ленивая загрузка модели (Lazy Loading)

Сервис использует **ленивую загрузку** для эффективного использования GPU памяти:

- Модель **не загружается** при старте сервиса
- Модель загружается автоматически при **первом запросе** к `/extract` или `/extract_file`
- После периода неактивности (по умолчанию 5 минут) модель **автоматически выгружается** из памяти
- При новом запросе модель загружается снова

### Преимущества:

- **Экономия GPU памяти** - модель не занимает ресурсы когда не используется
- **Быстрый старт** - сервис запускается мгновенно
- **Гибкость** - таймаут можно настроить через `MODEL_IDLE_TIMEOUT` (0 = отключить авто-выгрузку)

### Мониторинг состояния модели:

```bash
# Проверить загружена ли модель
curl http://localhost:8888/health | jq '.model_loaded'

# Если model_loaded: true - модель в памяти
# Если model_loaded: false - модель выгружена (загрузится при запросе)
```

## 📡 API Эндпоинты

### `GET /` - Информация о сервисе
```json
{
  "service": "BookNLP GPU Service",
  "version": "1.1.0",
  "model": "big",
  "status": "ready",
  "gpu": true,
  "lazy_loading": true,
  "idle_timeout_seconds": 300,
  "model_loaded": false
}
```

### `GET /health` - Проверка здоровья и GPU
```json
{
  "status": "healthy",
  "service": "BookNLP GPU Service",
  "model": "big",
  "gpu_enabled": true,
  "model_loaded": true,
  "cuda_available": true,
  "gpu_count": 1,
  "gpu_name": "NVIDIA GeForce RTX 3090",
  "gpu_memory_used_mb": 2048.5,
  "gpu_memory_cached_mb": 4096.0
}
```

### `POST /extract` - Извлечение сущностей
```json
{
  "text": "Frodo Baggins lived in the Shire...",
  "book_id": "lotr",
  "pipeline": "entity,quote,supersense,event,coref"
}
```

### `POST /model/load` - Ручная загрузка модели
Загружает модель в GPU память. Обычно это происходит автоматически при первом запросе.

```bash
curl -X POST http://localhost:8888/model/load
```

### `POST /model/unload` - Ручная выгрузка модели
Выгружает модель из GPU памяти для освобождения ресурсов.

```bash
curl -X POST http://localhost:8888/model/unload
```

## 🔧 Управление

```bash
# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Обновление
docker compose up -d --build
```

## 📂 Монтирование моделей

Модели хранятся на хосте и монтируются в контейнер:

```yaml
volumes:
  - ${MODELS_PATH:-./models}:/models
```

Это позволяет:
- Управлять моделями через файловую систему хоста
- Делать бэкапы
- Обновлять модели без пересборки контейнера

## 🔗 Интеграция с gstory

Из gstory (в LXC) использовать клиент:

```python
import requests

class RemoteBookNLPClient:
    def __init__(self, base_url: str = "http://HOST_IP:8888"):
        self.base_url = base_url

    def extract(self, text: str, book_id: str):
        response = requests.post(
            f"{self.base_url}/extract",
            json={"text": text, "book_id": book_id}
        )
        return response.json()
```
